{
  "name": "404",
  "version": "0.0.1",
  "scripts": {
    "serve": "npx vite --port 8080",
    "build": "npx vite build",
    "deploy": "firebase deploy --only hosting:zca-url-shortener-404"
  },
  "devDependencies": {
    "vite": "^2.9.13",
    "vite-plugin-minify": "^1.3.1"
  }
}
